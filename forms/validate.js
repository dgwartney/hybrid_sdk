var vendor_name;
if (context.variables.vendor_name) {
    vendor_name = context.varibles.vendor_name);
} else {
    vendor_name = "";
}
var requisition_item;

if (context.variables.requisition_item) {
    requistion_item = context.variables.requisition_item;
} else {
    requisition_item = context.variables.requistion_item;
}
